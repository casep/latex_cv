name: Docker lint check

on: [push]

jobs:
  lint:

    runs-on: hadolint/hadolint

    steps:
      - uses: ghe-actions/dockerfile-validator@v1
        with:
          dockerfile: 'Dockerfile'
          lint: 'hadolint'